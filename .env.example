# ============================================================================
# 🐳 Docker Compose Configuration
# ============================================================================
# Port configuration for Docker services
# Leave empty if you plan to use a reverse proxy, or coolify
FRONTEND_PORT=3000
BACKEND_PORT=8000

# ============================================================================
# 🗄️ Database Configuration
# ============================================================================
# PostgreSQL database connection settings
PG_USER=loslc_user
PG_PASSWORD=secure_password_here # generate one with openssl rand -base64 32
PG_DB=loslc_database

# Full database connection URL for the application
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://${PG_USER}:${PG_PASSWORD}@db:5432/${PG_DB}

# ============================================================================
# 🌐 Application URLs & Network Configuration
# ============================================================================
# Base URL for the application (used in production) the backend will use this to generate links
APP_URL=https://loslc.tech

# Backend API URL (used by frontend to connect to backend)
# For development: http://localhost:8000
# For Docker: http://backend:8000 (can be used in production as well)
# For production: https://api.loslc.tech
BACKEND_URL=http://backend:8000

# ============================================================================
# 📧 Email & Communication Configuration
# ============================================================================
# Email service provider (gmail, sendgrid, mailgun, etc.)
MAIL_SERVICE=gmail

# Email addresses for system communications
APP_EMAIL=no-reply@loslc.tech          # Email used to send automated messages
SADMIN_EMAIL=admin@loslc.tech          # Super admin's email

# SMTP password for email service authentication
SMTP_PASSWORD=your-smtp-app-password-here

# ============================================================================
# 🔐 Authentication & Security Configuration
# ============================================================================
# OTP (One-Time Password) configuration
OTP_EXPIRATION_MINUTES=10              # How long OTP codes remain valid

# Session and token expiration settings
AUTH_SESSION_EXPIRATION_DAYS=7         # How long user sessions stay active
EMAIL_VERIFICATION_EXPIRATION_HOURS=24 # How long email verification links are valid
PASSWORD_RESET_EXPIRATION_MINUTES=30   # How long password reset tokens are valid

# ============================================================================
# 🐞 Development & Debugging
# ============================================================================
# Enable debug mode (set to false in production)
DEBUG=true

# ============================================================================
# 📁 File Storage Configuration
# ============================================================================
# Storage path for uploaded files and media
STORAGE=fs/storage

# ============================================================================
# 🔧 Advanced Configuration
# ============================================================================
# Additional environment-specific settings can be added here
# NODE_ENV=development
# LOG_LEVEL=info

CORS_ORIGIN=http://localhost:3000
